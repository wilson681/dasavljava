/**
 * ECB Control layer: orchestrates use cases, creates entities and calls the
 * team AVL through SearchTreeInterface.
 * Each member owns one Control class for their approved module.
 */
package control;